import React, { useEffect } from 'react';

function TrendingSongs() {

  useEffect(() => {
    console.log('Component mounted'); // runs once on mount
  }, []); // ← empty array = run only on first render

  return <h2>Trending Songs 🎵</h2>;
}

export default TrendingSongs;



import React, { useState, useEffect } from 'react';

function IPLScoreFetcher() {
  const [headline, setHeadline] = useState('');

  useEffect(() => {
    fetch('https://jsonplaceholder.typicode.com/posts')
      .then((res)  => res.json())
      .then((data) => {
        setHeadline(data[0].title); // take first post's title
      });
  }, []); // [] = fetch only on mount

  return (
    <div>
      <h2>Match Headline</h2>
      <p>{headline || 'Loading...'}</p>
    </div>
  );
}

export default IPLScoreFetcher;




  
